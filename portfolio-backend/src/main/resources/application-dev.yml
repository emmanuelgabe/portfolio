# Development environment configuration
app:
  jwt:
    # Development: Comfortable expiration for local development
    access-token-expiration: 3600000      # 1 hour (60 minutes)
    refresh-token-expiration: 86400000    # 24 hours
  rate-limit:
    contact:
      max-requests-per-hour: 50  # Higher limit for development testing
    auth:
      login:
        max-requests-per-hour: 100  # Higher limit for development testing
      refresh:
        max-requests-per-hour: 200  # Higher limit for development testing

spring:
  jpa:
    generate-ddl: false  # Flyway manages schema creation
    hibernate:
      ddl-auto: validate  # Only validate schema, Flyway handles migrations
    open-in-view: false
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  application:
    name: portfolio-backend
  datasource:
    username: ${SPRING_DATASOURCE_USERNAME:postgres_app}
    password: ${SPRING_DATASOURCE_PASSWORD:password}
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://portfolio-db:5432/portfolio}
    driver-class-name: org.postgresql.Driver
  flyway:
    enabled: true
    baseline-on-migrate: true
    baseline-version: 0
    locations: classpath:db/migration
    validate-on-migrate: true
    clean-disabled: true  # Prevent accidental data deletion

server:
  port: 8080

logging:
  level:
    org.springframework.web: INFO
    org.hibernate: INFO
    com.emmanuelgabe.portfolio: DEBUG

# Springdoc OpenAPI (Swagger UI)
springdoc:
  api-docs:
    path: /api-docs
    enabled: true
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
    operationsSorter: method
    tagsSorter: alpha
    tryItOutEnabled: true
  show-actuator: true


management:
  endpoints:
    web:
      exposure:
        include: "*"  # Expose all actuator endpoints for local dev
      base-path: /actuator
  endpoint:
    health:
      show-details: always
      probes:
        enabled: true
  health:
    db:
      enabled: true
    diskspace:
      enabled: true
  info:
    build:
      enabled: true
    env:
      enabled: true
    java:
      enabled: true
    os:
      enabled: true
