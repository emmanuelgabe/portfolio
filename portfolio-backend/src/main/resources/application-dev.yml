spring:
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update  # Auto-create/update schema for local dev
    open-in-view: false
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true  # Show SQL queries in console for debugging
  application:
    name: portfolio-backend
  datasource:
    username: ${SPRING_DATASOURCE_USERNAME:postgres_app}
    password: ${SPRING_DATASOURCE_PASSWORD:password}
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://portfolio-db:5432/portfolio_local}
    driver-class-name: org.postgresql.Driver

server:
  port: 8080

logging:
  level:
    org.springframework.web: DEBUG
    org.hibernate: INFO
    com.emmanuelgabe.portfolio: DEBUG

# Development specific configs
management:
  endpoints:
    web:
      exposure:
        include: "*"  # Expose all actuator endpoints for local dev
      base-path: /actuator
  endpoint:
    health:
      show-details: always
      probes:
        enabled: true
  health:
    db:
      enabled: true
    diskspace:
      enabled: true
  info:
    build:
      enabled: true
    env:
      enabled: true
    java:
      enabled: true
    os:
      enabled: true
